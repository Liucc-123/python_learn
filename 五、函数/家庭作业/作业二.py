# 题目：编写一个名为 calculate_area() 的函数，它接收两个参数 length 和 width（假设都是数字），计算并返回矩形的面积（length * width）。
# 不要直接在函数内部打印结果。
def calculate_area(length: int, width: int):
    return length * width

print(calculate_area(4, 5))